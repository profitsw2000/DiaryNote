<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/main">

    <fragment
        android:id="@+id/main"
        android:name="diarynote.mainfragment.presentation.view.MainFragment"
        android:label="Главная"
        tools:layout="@layout/fragment_main"/>

    <fragment
        android:id="@+id/categories"
        android:name="diarynote.notesactivity.presentation.CategoriesFragment"
        android:label="Категории"
        tools:layout="@layout/fragment_categories"/>

    <fragment
        android:id="@+id/calendar"
        android:name="diarynote.notesactivity.presentation.CalendarFragment"
        android:label="Календарь"
        tools:layout="@layout/fragment_calendar"/>

    <fragment
        android:id="@+id/settings"
        android:name="diarynote.notesactivity.presentation.SettingsFragment"
        android:label="Настройки"
        tools:layout="@layout/fragment_settings"/>

</navigation>